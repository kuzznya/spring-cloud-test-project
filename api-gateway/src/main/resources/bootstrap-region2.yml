eureka:
  instance:
    hostname: peer2
    metadata-map:
      zone: zone2
  client:
    region: region2
    service-url:
      defaultZone: ${EUREKA_SERVER2:http://peer2:8762/eureka}
    availability-zones:
      region1: zone1
    fetch-remote-regions-registry: region1